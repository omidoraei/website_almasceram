# 🎨 سند مرجع سیستم طراحی و هویت بصری الماس سرام (ALMAS CERAM)
### Design System & Visual Guidelines Specification
**نام فایل:** `design.md`  
**نسخه:** `3.5.0`  
**تاریخ بروزرسانی:** سال ۲۰۲۵  
**جهت‌گیری طراحی:** لوکس معمارانه (Architectural Dark Slate & Almas Gold)  
**برندهای مرجع صنعت:** Porcelanosa, Atlas Concorde, Florim, Cotto d'Este  

---

## 📋 فهرست مندرجات
1. [بخش ۱: شخصیت برند و کلمات کلیدی بصری](#بخش-۱-شخصیت-برند)
2. [بخش ۲: سیستم پالت رنگی (Color Tokens)](#بخش-۲-سیستم-پالت-رنگی)
3. [بخش ۳: سیستم تایپوگرافی و فونت‌ها (Typography)](#بخش-۳-سیستم-تایپوگرافی)
4. [بخش ۴: سیستم فاصله‌گذاری و گرید (Spacing & Grid)](#بخش-۴-سیستم-فاصله‌گذاری)
5. [بخش ۵: راهنمای بصری کامپوننت‌های فرانت‌اند](#بخش-۵-راهنمای-کامپوننت‌ها)
6. [بخش ۶: انیمیشن‌ها و Micro-interactions](#بخش-۶-انیمیشن‌ها)
7. [بخش ۷: جدول Design Tokens برای توسعه‌دهندگان](#بخش-۷-جدول-tokens)

---

## 🏛️ بخش ۱: شخصیت برند و کلمات کلیدی بصری (Brand Personality)

طراحی بصری وب‌سایت **الماس سرام** بر پایه ۴ رکن اصلی معماری لوکس استوار است:

1. **مدرن و استوار (Solid Architectural):** خطوط صاف، فریم‌های دیسیپلین‌دار و زوایای مهندسی‌شده که یادآور برش‌های لیزری (Rectified) سرامیک و اسلب است.
2. **لوکس و اصیل (Premium Luxury):** پس‌زمینه‌های تیره عمیق (Dark Slate) به همراه لمس‌های طلایی درخشان (Almas Gold) که جلا و انعکاس لعاب‌های سوپر پولیش را برجسته می‌سازد.
3. **شفاف و فنی (Technical Clarity):** نمایش خوانا و جدول‌بندی‌شده داده‌های آزمایشگاهی ISO 10545، جذب آب زیر ۰.۱٪ و ابعاد دقیق.
4. **مینیمالیسم پالایش‌شده (Refined Minimalism):** استفاده هوشمندانه از فضاهای منفی (Negative Space) و جلوگیری از شلوغی بصری.

---

## 🎨 بخش ۲: سیستم پالت رنگی (Color Tokens)

پالت رنگی پروژه برای رندر عالی روی نمایشگرهای OLED و 4K با کدهای دقیق HEX و کلاس‌های Tailwind تنظیم شده است:

### ۲.۱. رنگ‌های اصلی پس‌زمینه (Background & Surface)
* **`Primary Dark Background` (پس‌زمینه اصلی سایت):**  
  `#090D16` | Tailwind: `bg-slate-950`  
  *کاربرد:* پس‌زمینه اصلی تمام صفحات، هدر و فوتر. ایجاد عمق بصری معمارانه.
* **`Surface Card Background` (پس‌زمینه کارت‌ها و مدال‌ها):**  
  `#0F172A` | Tailwind: `bg-slate-900`  
  *کاربرد:* پس‌زمینه کارت‌های محصول، مدال‌های جزئیات، فرم‌ها و پنل مدیریت.
* **`Subtle Border Color` (خطوط تفکیک و فریم‌ها):**  
  `#1E293B` | Tailwind: `border-slate-800`  
  *کاربرد:* خطوط جداکننده نازک بین بخش‌ها، فریم کارت‌ها و جداول فنی.

### ۲.۲. رنگ‌های برندینگ و تاکید (Accent & Brand Colors)
* **`Almas Gold Primary` (طلایی اصلی برند):**  
  `#F59E0B` | Tailwind: `bg-amber-500` / `text-amber-500` / `border-amber-500`  
  *کاربرد:* دکمه‌های اصلی اقدام (CTA)، بج‌های ویژه، حاشیه‌های فعال و آیکون‌های شاخص.
* **`Almas Gold Bright` (طلایی روشن درخشان):**  
  `#FBBF24` | Tailwind: `text-amber-400` / `bg-amber-400`  
  *کاربرد:* متون تیتر برجسته، کدهای کالا، بج‌های سایز و حالت‌های Hover.
* **`Emerald Success` (سبز زمردی استعلام و واتساپ):**  
  `#10B981` | Tailwind: `bg-emerald-500` / `text-emerald-400`  
  *کاربرد:* دکمه شناور واتساپ، بج جذب آب زیر ۰.۱٪ و تایید استعلام موفق.

### ۲.۳. سیستم متون و خنثی (Typography Text Palette)
* **`Text Primary Heading` (تیترهای اصلی):** `#F8FAFC` (`Slate 50`) - کنتراست ۱۰۰٪ برای خوانایی تیترها.
* **`Text Secondary Body` (متون توضیحات):** `#CBD5E1` (`Slate 300`) - متون اصلی توضیحات محصولات و فرم‌ها.
* **`Text Muted Specs` (مشخصات فنی و فرعی):** `#94A3B8` (`Slate 400`) - کدهای کالا، تاریخ‌ها و برچسب‌های راهنما.

---

## ✏️ بخش ۳: سیستم تایپوگرافی و فونت‌ها (Typography System)

### ۳.۱. استراتژی فونت‌های چندزبانه
* **زبان فارسی (`fa`) و عربی (`ar`) - `RTL`:**
  * **فونت تیترها و عناوین (Headings):** **`Peyda`** / **`Yekan Bakh`** با وزن‌های `Extrabold 800` و `Black 900`.
  * **فونت متون، فرم‌ها و جداول فنی (Body & Specs):** **`Vazirmatn`** (فونت Variable مدرن) با پشتیبانی کامل از اعداد فارسی و انگلیسی.
* **زبان انگلیسی (`en`) - `LTR`:**
  * **فونت تیترها و متون:** **`Plus Jakarta Sans`** / **`Inter`** / **`Monospace`** برای کدهای فنی.

### ۳.۲. مقیاس اندازه‌ها و Line-Height (Typography Scale)

| سطح تایپوگرافی | اندازه (Pixels) | وزن (Weight) | Line Height | کاربرد در UI |
| :--- | :--- | :--- | :--- | :--- |
| **Display Hero (H1)** | `56px` (`text-5xl`/`text-6xl`) | `Black 900` | `1.15` | تیتر اصلی بالای صفحه اصلی (Hero Title) |
| **Section Heading (H2)** | `36px` (`text-3xl`/`text-4xl`) | `Extrabold 800` | `1.25` | عناوین اصلی بخش‌های کاتالوگ و گالری |
| **Subtitle / Card Title (H3)**| `18px` (`text-lg`/`text-xl`) | `Bold 700` | `1.4` | نام محصولات در کارت‌ها و مدال‌ها |
| **Body Text** | `14px` (`text-xs`/`text-sm`) | `Regular 400` / `Light 300` | `1.7` | پاراگراف‌ها، توضیحات برند و فرم‌ها |
| **Technical Specs & Badges** | `11px` (`text-[11px]`) | `Monospace Bold 700` | `1.2` | کدهای کالا، ابعاد CM، درصد جذب آب |

---

## 📏 بخش ۴: سیستم فاصله‌گذاری و گرید (Spacing & Grid System)

پروژه از سیستم فاصله‌گذاری پایه **۸ پیکسل (8px Base Unit)** پیروی می‌کند:

* `space-1` (`4px`): فاصله بین آیکون و متن برچسب‌ها.
* `space-2` (`8px`): پدینگ داخلی بج‌های سایز و نوع سطح.
* `space-3` (`12px`): پدینگ داخلی دکمه‌های معمولی و ورودی‌های فرم.
* `space-4` (`16px`): پدینگ کارت‌های محصول و فاصله بین فیلترها.
* `space-6` (`24px`): پدینگ داخلی مدال‌های جزئیات و بخش‌های اصلی.
* `space-10` (`40px`): فاصله بین بخش‌های اصلی در صفحه اصلی.
* `space-16` (`64px`): پدینگ بالایی و پایینی سکشن‌های بزرگ.

### ۴.۱. واکنش‌گرایی گرید محصولات (Responsive Grid System)
* **دسکتاپ (Desktop ≥ 1024px):** ۳ تا ۴ ستون همراه با منوی فیلتر چسبان در سمت راست (Sidebar).
* **تبلت (Tablet 640px - 1023px):** ۲ ستون منظم.
* **موبایل (Mobile < 640px):** ۱ ستون عریض همراه با کشوی فیلترها و نوار سایزهای افقی قابل اسکرول.

---

## 🛠️ بخش ۵: راهنمای بصری کامپوننت‌های فرانت‌اند (Component Visuals)

### ۵.۱. Header / Navigation Bar
* **استایل پس‌زمینه:** `bg-slate-950/90 backdrop-blur-xl border-b border-amber-500/20`
* **نوار بالای هدر:** نوار باریک گرادینتی تیره با متون طلایی خنثی شامل لوگوی ISO 10545، سوئیچر زبان پرچم‌دار و لینک‌های سریع.
* **لوگوی برند:** جعبه مربعی گرادینتی طلایی (`from-amber-300 via-amber-500 to-amber-700`) با حروف `AC` مشکی و متن درخشان `ALMAS CERAM`.
* **مگامنو (Mega Menu):** جعبه شیشه‌ای شناور شامل کارت‌های پیش‌نمایش تصویری ۷ سایز اصلی همراه با تصویر نمونه کاشی، توضیحات و لینک فیلتر مستقیم.
* **نوار میانبر سایزها:** نوار افقی چسبان با دکمه‌های بیضی‌شکل (`rounded-full`)؛ سایز فعال دارای رینگ طلایی و پس‌زمینه درخشان (`bg-amber-400 text-slate-950`).

### ۵.۲. Hero Section (بخش اول هوم‌پیدج)
* **استیج بصری:** پس‌زمینه تیره با نورهای محیطی آمبینت طلایی و قهوه‌ای در پس‌زمینه (`blur-[120px]`).
* **کارت پیش‌نمایش شناور:** کارت عکس اسلب اونیکس رویال با حاشیه گرادینتی طلایی و بج زنده تعداد محصولات دیتابیس.
* **کارت‌های شاخص‌های کیفی:** ۴ کارت کوچک شیشه‌ای شامل درصد جذب آب زیر ۰.۱٪، تعداد سایزها، تنوع فیس‌ها و برش رکتیفاید.

### ۵.۳. شبکه بصری سایزها (Size Grid Showcase)
* **طراحی بنتو گرید (Bento Grid):** کارت‌های تصویری با نسبت ابعاد `16:11` برای ۷ سایز (`100x100`, `60x120`, `80x80`, `60x60`, `30x90`, `40x40`, `30x30`).
* **تکنیک تصویر:** تصویر نصب‌شده کاشی در محیط، اورلی گرادینت تیره از پایین به بالا، بج سایز در بالا سمت راست و عنوان فارسی/انگلیسی در پایین.

### ۵.۴. کارت محصول (Product Card)
* **فریم کارت:** `bg-slate-900/90 border border-slate-800 rounded-2xl overflow-hidden shadow-xl`
* **تصویر محصول:** نسبت ابعاد `4:3` با افکت هوور زوم روان (`group-hover:scale-105 transition-transform duration-700`).
* **بج‌های شناور روی عکس:**
  * بالا سمت راست: بج سایز با فونت مونو (`60x120 cm`).
  * بالا سمت چپ: بج نوع سطح (`پولیش سوپر براق`).
  * پایین سمت راست: بج تعداد فیس‌های متغیر (`۸ فیس متغیر`).
* **دکمه‌های اقدام:** دکمه اصلی طلایی «افزودن به استعلام» و دکمه آیکونی مقایسه.

### ۵.۵. مدال جزئیات محصول (Product Detail Modal)
* **مسیر ناوبری (Breadcrumbs):** مسیر متنی باریک در بالای مدال (`صفحه اصلی > محصولات > سایز ۶۰x۱۲۰ > نام محصول`).
* **استیج گالری تصویر:**
  * نمایش تصویر اصلی با قابلیت زوم/بزرگنمایی بافت.
  * نوار سوییچر فیس‌های متغیر (نمایش دکمه‌های کوچک فیس #۱ تا #N با بج شماره).
  * سوییچر رندر دکوراسیون داخلی.
* **جدول مشخصات فنی ISO:** جدول منسجم شامل ابعاد، نوع سطح، بدنه پرسلان، تعداد فیس، ضخامت mm و جذب آب.
* **خوشه دکمه‌های اقدام (CTA Cluster):** دکمه افزودن به پیش‌فاکتور، دکمه چت مستقیم واتساپ با پیام پیش‌فرض محصول، دکمه دانلود کاتالوگ PDF و دکمه مقایسه.

### ۵.۶. دکمه شناور واتساپ (Floating WhatsApp Button)
* **طراحی بصری:** دکمه دایره‌ای زمردی (`bg-emerald-500 hover:bg-emerald-600`) با حاشیه درخشان و حلقه پالس انیمیشنی (`animate-pulse`).
* **حباب پیام هوشمند (Context Tooltip):** حباب شیشه‌ای بالای دکمه که نام کاشی بازشده را نمایش داده و با کلیک، پیام آماده اختصاصی همان محصول را به واتساپ ارسال می‌کند.

### ۵.۷. پنل مدیریت ادمین (Admin Panel & Sheet Editor)
* **داشبورد تیره لوکس:** کارت‌های KPI شاخص‌ها با اعداد مونو درخشان.
* **ویرایشگر اکسل آنلاین (Sheet Editor):** جدول تعاملی شبیه اکسل برای ویرایش مستقیم سلول‌ها، Find & Replace متون و اعمال تغییرات با دکمه Batch Save.
* **ویرایشگر چندزبانه (Product Form Modal):** فرم مدال دارای تب‌های زبانی (`🇮🇷 فارسی` | `🇬🇧 English` | `🇸🇦 العربية`).
* **جدول محصولات:** نمایش بج‌های رنگی تکمیل ترجمه (سبز = کامل، زرد = نیمه‌کامل، طوسی = فقط فارسی).

---

## ⚡ بخش ۶: انیمیشن‌ها و Micro-interactions

1. **انیمیشن اسکرول هدر:** پنهان‌شدن هوشمند هنگام اسکرول به پایین و نمایان‌شدن روان هنگام اسکرول به بالا (`transition-transform duration-300`).
2. **افکت Hover کارت‌ها:** زوم ملایم ۱۰۵٪ تصویر کاشی همراه با درخشش حاشیه طلایی (`hover:border-amber-500/60`).
3. **اسکلتون لودینگ (Skeleton Screen):** کارت‌های خاکستری انیمیشنی موج‌دار (`animate-pulse`) هنگام دریافت داده‌ها از دیتابیس Supabase.
4. **دکمه پالس واتساپ:** افکت پالس هاله سبز زمردی در گوشه پایین صفحه.
5. **قاعده Accessibility:** پشتیبانی از `prefers-reduced-motion` در CSS اصلی برای غیرفعال کردن انیمیشن‌های شدید جهت کاربران دارای حساسیت بصری:
   ```css
   @media (prefers-reduced-motion: reduce) {
     *, ::before, ::after {
       animation-duration: 0.01ms !important;
       transition-duration: 0.01ms !important;
       scroll-behavior: auto !important;
     }
   }
   ```

---

## 📊 بخش ۷: جدول Design Tokens برای توسعه‌دهندگان (Tailwind CSS Mapping)

| دسته‌بندی Token | نام متغیر / کلاس Tailwind | مقدار دقیق (Hex / Value) | عنصر استفاده‌شده در UI |
| :--- | :--- | :--- | :--- |
| **Color** | `bg-slate-950` | `#090D16` | پس‌زمینه اصلی صفحات، هدر و فوتر |
| **Color** | `bg-slate-900` | `#0F172A` | پس‌زمینه کارت‌های محصول، مدال‌ها و فرم‌ها |
| **Color** | `border-slate-800` | `#1E293B` | حاشیه‌ها، فریم‌ها و خطوط جداکننده |
| **Color** | `bg-amber-500` / `text-amber-400` | `#F59E0B` / `#FBBF24` | رنگ اصلی برند، متراژ، کدهای کالا و CTA |
| **Color** | `bg-emerald-500` | `#10B981` | دکمه شناور واتساپ و بج جذب آب |
| **Color** | `text-slate-50` | `#F8FAFC` | تیترهای اصلی با کنتراست کامل |
| **Color** | `text-slate-400` | `#94A3B8` | مشخصات فنی، کدهای کالا و متون راهنما |
| **Font Family** | `font-sans` | `Peyda, Yekan Bakh, Vazirmatn` | متون فارسی و عربی (RTL) |
| **Font Family** | `font-mono` | `Plus Jakarta Sans, Inter, monospace` | متون انگلیسی (LTR)، کدهای کالا و ابعاد |
| **Border Radius** | `rounded-2xl` | `16px` | کارت‌های کاتالوگ و مدال‌ها |
| **Border Radius** | `rounded-xl` | `12px` | دکمه‌های اقدام و ورودی‌های فرم |
| **Border Radius** | `rounded-full` | `9999px` | بج‌های سایز و دکمه واتساپ |
| **Shadow** | `shadow-2xl` | `0 25px 50px -12px rgba(0,0,0,0.7)` | سایه عمیق معمارانه کارت‌ها و مدال‌ها |

---

**تاییدیه نهایی:** فایل `design.md` ریشه پروژه مرجع کامل ساختار بصری، رنگ‌بندی، تایپوگرافی و قالب کامپوننت‌های **الماس سرام** است و هر طراح یا برنامه‌نویسی در آینده با مطالعه آن، زبان بصری پروژه را به طور کامل درک خواهد کرد.
