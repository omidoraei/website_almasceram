# 📘 راهنمای جامع اجرای پایگاه داده الماس سرام

## ✅ ویژگی‌های این نسخه
- **ساختار تمیز و مهندسی شده**: حذف پیچیدگی‌های غیرضروری
- **امنیت کامل (RLS)**: سیاست‌های دسترسی دقیق برای کاربران عادی و ادمین
- **داده‌های نمونه واقعی**: تمام جداول با داده‌های فارسی و مرتبط پر شده‌اند
- **رفع باگ search_path**: تابع `update_updated_at_column` کاملاً ایمن شده است
- **تریگرهای خودکار**: فیلد `updated_at` به صورت خودکار بروزرسانی می‌شود

---

## 📋 مراحل اجرا (گام به گام)

### مرحله 1: ورود به پنل Supabase
1. به آدرس [https://supabase.com](https://supabase.com) بروید.
2. وارد حساب کاربری خود شوید.
3. پروژه **الماس سرام** را انتخاب کنید.

### مرحله 2: باز کردن SQL Editor
1. از منوی سمت چپ، روی گزینه **SQL Editor** کلیک کنید.
2. روی دکمه **+ New Query** کلیک کنید تا یک صفحه خالی باز شود.

### مرحله 3: کپی و اجرای اسکریپت
1. فایل `supabase-clean-schema.sql` را در کامپیوتر خود باز کنید.
2. **تمام محتوای فایل** را کپی کنید (Ctrl+A سپس Ctrl+C).
3. محتوای کپی شده را در ادیتور SQL در سایت Supabase پیست کنید (Ctrl+V).
4. روی دکمه **Run** (پایین سمت راست) کلیک کنید.

### مرحله 4: بررسی نتیجه
- اگر همه چیز درست باشد، پیام سبز رنگ **"Success. No rows returned"** را مشاهده می‌کنید.
- هیچ خطایی نباید وجود داشته باشد.

### مرحله 5: تأیید جداول
1. از منوی سمت چپ روی **Table Editor** کلیک کنید.
2. باید جداول زیر را ببینید:
   - ✅ `site_settings` (تنظیمات سایت)
   - ✅ `sliders` (اسلایدرها)
   - ✅ `collections` (دسته‌بندی‌ها)
   - ✅ `products` (محصولات)
   - ✅ `standards` (استانداردها)
   - ✅ `portfolio_items` (نمونه کارها)
   - ✅ `homepage_settings` (محتوای صفحه اصلی)
   - ✅ `contact_requests` (پیام‌های تماس)
   - ✅ `admin_logs` (لاگ‌های مدیریت)

3. روی هر جدول کلیک کنید و تب **Data** را بزنید تا مطمئن شوید داده‌های نمونه وارد شده‌اند.

---

## 🔍 ساختار جداول (خلاصه فنی)

| نام جدول | توضیح | دسترسی عمومی | دسترسی ادمین |
| :--- | :--- | :--- | :--- |
| `site_settings` | تنظیمات کلی مثل عنوان و فوتر | فقط خواندن (SELECT) | کامل (ALL) |
| `sliders` | اسلایدرهای صفحه اصلی | فقط خواندن | کامل |
| `collections` | دسته‌بندی محصولات | فقط خواندن | کامل |
| `products` | لیست محصولات با جزئیات فنی | فقط خواندن | کامل |
| `standards` | گواهینامه‌ها و استانداردها | فقط خواندن | کامل |
| `portfolio_items` | پروژه‌های انجام شده | فقط خواندن | کامل |
| `homepage_settings` | محتوای داینامیک (JSON) | فقط خواندن | کامل |
| `contact_requests` | فرم تماس | فقط ارسال (INSERT) | خواندن (SELECT) |
| `admin_logs` | لاگ تغییرات | ❌ بسته | کامل |

---

## 🛠️ نکات مهم و عیب‌یابی

### 1. خطای "relation already exists"
اگر این خطا را گرفتید، یعنی جداول قبلی هنوز وجود دارند.
- **راه حل:** نگران نباشید، دستور `DROP TABLE IF EXISTS` در ابتدای اسکریپت وجود دارد و باید آن‌ها را پاک کند. اگر باز هم خطا داد، به بخش **SQL Editor** بروید و دستی جداول را حذف کنید یا از دستور `DROP SCHEMA public CASCADE; CREATE SCHEMA public;` استفاده کنید (⚠️ هشدار: این دستور کل دیتابیس را پاک می‌کند).

### 2. عدم نمایش داده‌ها در سایت
اگر جداول ساخته شدند اما سایت چیزی نشان نمی‌دهد:
- به بخش **Authentication > Policies** در Supabase بروید.
- مطمئن شوید که Policyهای مربوط به `SELECT` برای نقش `anon` و `public` فعال باشند (در اسکریپت ما با `FOR SELECT USING (true)` این کار انجام شده است).
- کش مرورگر را پاک کنید و سایت را رفرش کنید.

### 3. خطای Function Search Path
این مشکل در این نسخه **کاملاً رفع شده است**. خط `SET search_path = pg_catalog, public;` در تعریف تابع اضافه شده تا از تداخل اسکماها جلوگیری کند.

---

## 🚀 اقدام بعدی: اتصال به پروژه
پس از اطمینان از ساخت دیتابیس:
1. به پنل **Vercel** بروید.
2. پروژه را پیدا کرده و به بخش **Settings > Environment Variables** بروید.
3. مطمئن شوید متغیر `NEXT_PUBLIC_SUPABASE_URL` و `NEXT_PUBLIC_SUPABASE_ANON_KEY` صحیح هستند.
4. پروژه را **Redeploy** کنید (تب Deployments > منوی سه نقطه > Redeploy).

---

## 📞 پشتیبانی
اگر در هر مرحله به مشکلی برخوردید، گزارش خطا (Screenshot) را ارسال کنید تا بررسی شود.
